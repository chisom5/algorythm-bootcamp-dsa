# Tree II 

This topic cover the other part that complete the topic on tree data structure. which is "Breadth-first search" and "Binary search tree".

- Breadth-firs search: walk a tree one level at a time and it uses queue. questions here involves rows of a tree rather than paths through it: print each level, find the shallowest leaf, read the tree from the side.

- Binary search trees: Add a single rule where values are allowed to live.